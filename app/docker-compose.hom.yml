version: "3.8"

services:
  portal-integra-hom:
    build:
      context: Docker
      dockerfile: Docker/Dockerfile.hom
    container_name: portal_integra_hom
    ports:
      - "8083:80"
    environment:
      TZ: America/Sao_Paulo
    restart: unless-stopped
